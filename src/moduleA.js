import *  as abc from '/src/moduleB.js';
export const sayHello = (name) => console.log(`Hello ${name}`);
export const sayBye = (name) => console.log(`Hello ${name}`);
const foo = 'bar';
export default foo;